"""
MCP Transport Layer — Communication with MCP servers for active probing.

Provides async transports for JSON-RPC communication with MCP servers
over stdio (subprocess) and SSE (HTTP). Used by Wedge 2 checks.
"""
