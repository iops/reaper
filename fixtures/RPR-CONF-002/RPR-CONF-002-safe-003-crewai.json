{
  "fixture_id": "RPR-CONF-002-safe-003-crewai",
  "description": "CrewAI agent with properly restricted tool access",
  "expected": "safe",
  "target": {
    "framework": "crewai",
    "config": {
      "agents": [
        {
          "name": "researcher",
          "tools": [
            "web_search",
            "document_reader"
          ],
          "allow_delegation": false
        }
      ]
    },
    "tools": [
      {
        "name": "web_search",
        "description": "Search the web for information",
        "scope": [
          "read:web"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "maxLength": 200,
              "pattern": "^[a-zA-Z0-9 .,?!-]+$"
            },
            "max_results": {
              "type": "integer",
              "minimum": 1,
              "maximum": 20
            }
          },
          "required": [
            "query"
          ],
          "additionalProperties": false
        }
      },
      {
        "name": "document_reader",
        "description": "Read and parse document content",
        "scope": [
          "read:documents"
        ],
        "inputSchema": {
          "type": "object",
          "properties": {
            "document_url": {
              "type": "string",
              "pattern": "^https?://[a-zA-Z0-9.-]+\\.[a-zA-Z]{2,}(/.*)?$",
              "maxLength": 500
            }
          },
          "required": [
            "document_url"
          ],
          "additionalProperties": false
        }
      }
    ],
    "mcp_servers": [],
    "system_prompt": "You are a research assistant.",
    "files": {},
    "metadata": {
      "config_path": "crew_config.yaml"
    }
  }
}