{
  "fixture_id": "RPR-CONF-002-border-002-langchain",
  "description": "Tool with minimal but present input constraints",
  "expected": "borderline",
  "target": {
    "framework": "langchain",
    "config": {
      "tools": [
        {
          "name": "web_search",
          "description": "Search the web for information",
          "scope": [
            "read:web"
          ],
          "args_schema": {
            "type": "object",
            "properties": {
              "query": {
                "type": "string",
                "maxLength": 500
              },
              "limit": {
                "type": "integer",
                "minimum": 1,
                "maximum": 20
              }
            },
            "required": [
              "query"
            ]
          }
        }
      ]
    },
    "tools": [],
    "mcp_servers": [],
    "system_prompt": null,
    "files": {},
    "metadata": {}
  }
}